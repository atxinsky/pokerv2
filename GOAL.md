# PokerGym Goal — LLM 高强度训练桌

> 状态：计划已定，代码稍后改。对应仓库：`atxinsky/pokerv2`（本地 `D:\pokerv2`）。
> 既有 v0.2（引擎 + 频率 bot + 可选 LLM 层 + 夜店风 UI）视为基线；本文件定义下一阶段产品目标。

## 一句话目标

把 PokerGym 做成 **充分利用大模型提高打牌技术** 的本机训练器：
- **对手**用 LLM 真决策（消耗 token，更像真人、更难搞）；
- **对自己**用 LLM 做教练（手后复盘 / 可选轻提示 / 错题与弱项 drill）；
- **前端**质感升级，配得上天天打开。

不是联网多人房、不是真钱、不是「只给 bot 加嘴炮」。

## 非目标（本阶段明确不做）

- 在线匹配 / 房间 / 账号体系
- 真钱或钱包
- 把静态 `gto.py` 宣传成 solver 级 GTO
- 用 LLM 替 hero 自动打牌（教练辅助，决策仍在人）

## 成功标准

1. 配置好 API Key 后，默认（或产品默认）**每个对手座位用 LLM 决策**；超时/失败回落现有频率 bot。
2. 桌上能感知「谁在思考」，不会像卡死。
3. 每手结束后有 **针对 hero 的 LLM 复盘**（错在哪、更好线、为什么）。
4. UI 明显好看一档：主舞台 + 右侧教练/思考面板 + 可用的复盘呈现。
5. 有训练 / 拟真等模式与用量（token/费用）可控开关。
6. 文案对齐：卖点是「LLM 对手 + LLM 教练」；GTO 表仅作参考辅助。

## 分阶段任务

### Phase 1 — 对手真 LLM（P0，优先）

- [x] 默认开启「全桌 LLM 决策」（有 Key 时）：每 bot 模型想动作 + 可选短台词
- [x] 合法动作约束（snap to legal）；失败/超时回落 A 层频率 bot
- [x] 人设/风格进 prompt（紧凶、松被动、鱼、REG 等），同桌多样
- [x] 成本与延迟：并行请求、超时预算、模型/强度档位、「几人用 LLM」可配
- [x] 设置持久化（沿用现有 settings / SQLite 模式）
- [x] API 暴露 thinking/busy，供前端展示
- [x] 非联网路径的测试（mock LLM）

### Phase 2 — 教练闭环（P1）

- [x] 手后自动 LLM 复盘（结合行动、底池、位置、对手类型）
- [x] 复盘可存储 / 回顾（错题本雏形）
- [x] 决策前可选轻提示（默认短或关，不替用户打）
- [x] 按弱项生成 drill 牌局（后续可增强）

### Phase 3 — 前端质感（与 Phase 1 可并行，P0 视觉）

- [x] 牌桌主舞台：更大更清晰牌面、筹码/行动高亮、街道节奏、座位布局
- [x] 「某某正在思考」状态，避免空白卡住
- [x] 右侧固定「教练 / 对手思考」面板
- [x] 设置页：对手 LLM 开关与强度、教练开关
- [x] 独立或内嵌复盘呈现：时间轴、备选线、「再讲细一点」

### Phase 4 — 产品打磨（P2）

- [x] 模式：训练（教练开）/ 拟真（少提示、对手更猛）
- [x] 用量面板：本局 token、预估费用、一键降档
- [x] README / SPEC 与现实对齐（C 层/LLM 关系、GTO 表述）
- [ ] 发布卫生：未提交脚本与文档整理（不阻塞功能）

## 建议开工顺序

`Phase1 对手 LLM → 思考中 UI / 桌面基础 → Phase2 手后复盘 → Phase3 复盘页打磨 → Phase4`

即：**先让对面变猛并好看一点，再加深复盘。**

## 技术约束（实现时遵守）

- 保持引擎正确性：`BotView` 信息隐藏、合法动作、现有 pytest
- 优先沿用 DeepSeek / `.env` / settings / `store.py` / `llm_brain.py` 路径，不新造云端栈
- 可继续 stdlib HTTP + vanilla `web/`；非必要不重写成大框架
- 仍是单机会话训练器；筹码为练习筹码

## 与旧文档关系

- `SPEC.md` / `task_plan.md`：v0.2 基线（多数 Phase 已勾完）
- **本文件 `GOAL.md`**：v0.3+ 产品目标与待办；代码改动以本文件为准推进
- 实现落地后：勾选上方 checkbox，并回写 README 一句产品定位

## 阻塞与备注

- Cursor Cloud Agent 当前套餐不可用；计划先入库，代码待 Pro 或明确本机改码授权后再做
- 本地曾有未提交改动：`README.md`、`pokergym/cli.py`、`scripts/`——实现时注意与远程 `master` 对齐

---

最后更新：2026-09-04（Phase4：训练/拟真模式 + 用量面板 + README/SPEC 对齐）
