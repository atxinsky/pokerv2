# PokerGym 开工规格（相对 PRD v0.2 的裁剪）

1. 不做 D 层，不维护两套复现逻辑。
2. C 层不调用 LLM，RNG 在 `ARCHETYPE_RANGE` 内偏移。
3. B 层纯规则；只适应英雄，bot 互不适应。
4. T3 只拦非法动作，不拦「翻牌下注-转牌过牌-河牌下注」这类真人线。
5. 统计验收按原型盒子，不按全体 15–60 VPIP。
6. 训练验收与拟人度分开：`drill` 看 3bet 是否朝剥削方向走。
