# PokerGym 规格摘要（相对 v0.2 / GOAL 对齐）

1. 引擎与频率 bot 为基线；静态 `gto.py` / 范围图 **仅作参考**，不宣传为 solver 级 GTO。
2. 产品主路径：**LLM 对手真决策** + **LLM 教练**（手后复盘 / 可选轻提示）；失败回落频率 bot。
3. C 层（人格嘴炮/适应文案）与出牌脑（`llm_brain`）共用 DeepSeek；密钥与牌谱仅本机。
4. 模式：`train` 教练友好；`realism` 少提示、LLM 覆盖更猛。
5. 用量：累计/本席 token 与费用粗估；可一键降低出牌强度。
6. T3 只测信息隔离与合法动作；统计仍按原型夹逼；弱项 drill 生成仍为后续项。
